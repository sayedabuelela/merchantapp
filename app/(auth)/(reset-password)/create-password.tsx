import ResetCreatePasswordScreen from "@/src/modules/auth/password/reset/reset-password.view";

const CreatePassword = () => {
    return (
        <ResetCreatePasswordScreen />
    );
};

export default CreatePassword;
