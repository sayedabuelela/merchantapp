import RegisterPasswordScreen from "@/src/modules/auth/register/register-password.view";

const RegisterPassword = () => {
    return (
        <RegisterPasswordScreen />
    );
}

export default RegisterPassword;
